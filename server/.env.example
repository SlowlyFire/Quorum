# Copy to server/.env and fill in. Never commit server/.env.

# Optional — these have development defaults (3000, development, http://localhost:5173).
PORT=
NODE_ENV=
CLIENT_URL=

# Optional, and both are for one deployment shape: client and API on subdomains
# of one apex. LEAVE BOTH EMPTY IN DEVELOPMENT.
#
#   CORS_ORIGINS   extra origins allowed to make credentialed calls, comma-
#                  separated. CLIENT_URL's origin is ALWAYS allowed and does not
#                  need repeating here. This is for a hostname migration — both
#                  the old and new client answered while the new one is being
#                  verified — and empty is the steady state. An origin left here
#                  afterwards can still make calls with a user's cookie.
#
#   COOKIE_DOMAIN  the session cookie's Domain, e.g. .askthequorum.com. SETTING
#                  IT ALSO MOVES SameSite FROM None BACK TO Lax, because Lax is
#                  correct exactly when every client is same-site with this API,
#                  which is what a shared cookie domain asserts. Set it only
#                  once the client is genuinely on a subdomain of this value —
#                  set early, every cross-site client stops sending the cookie
#                  and every user appears signed out.
CORS_ORIGINS=
COOKIE_DOMAIN=

# Required always. The process refuses to boot without them.
# JWT_SECRET must additionally be >= 32 characters when NODE_ENV=production.
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
# OPENROUTER_API_KEY is every LLM call in the product — get one at
# https://openrouter.ai/keys and set a hard spend cap on it in their dashboard.
DATABASE_URL=
JWT_SECRET=
OPENROUTER_API_KEY=

# Required only when NODE_ENV=production — nothing reads them yet.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Required only when NODE_ENV=production. Wallet top-ups need them; nothing else
# does, so the API boots without them and POST /api/wallet/checkout answers 503.
# TEST MODE — the integration is production-shaped, the keys are not real money.
#   STRIPE_SECRET_KEY     sk_test_... from https://dashboard.stripe.com/test/apikeys
#   STRIPE_WEBHOOK_SECRET whsec_... printed by `stripe listen --forward-to \
#                         localhost:3000/api/webhooks/stripe`, and DIFFERENT from
#                         the dashboard endpoint's secret — the CLI signs with its
#                         own. A mismatch is a 400 with "No signatures found".
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
