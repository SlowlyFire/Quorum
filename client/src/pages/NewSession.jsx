import { PagePlaceholder } from '../components/PagePlaceholder.jsx';

export function NewSession() {
  return (
    <PagePlaceholder title="Assemble your council" session={8}>
      Pick who answers, pick who judges, then ask. The council picker, the per-round cost estimate
      and the toggles from mockup 01 land with the debate view.
    </PagePlaceholder>
  );
}
